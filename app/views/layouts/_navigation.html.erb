<nav class="navbar navbar-expand-lg bg-body-tertiary mb-3">
  <div class="container-md">
    <a class="navbar-brand" href="/dashboard">Bündnerclub Zürich</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link <%= 'active' if current_page?(dashboard_path) %>" aria-current="page"
            href="<%= dashboard_path %>">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= 'active' if current_page?(students_path) %>" href="<%= students_path %>">Mitglieder</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= 'active' if current_page?(events_path) %>" href="<%= events_path %>">Events</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Statistik</a>
        </li>
      </ul>
      <ul class="navbar-nav d-flex flex-row">
        <li class="nav-item dropdown">
          <% if logged_in? %>
            <% if current_user.role==3%>
              <%= render "layouts/admin_dropdown" %>
            <% else %>
              <%= button_to "Abmelden", logout_path, method: :delete, class: "dropdown-item" %>
            <% end %>
          <% else %>
            <%= link_to "Not logged in" , root_path %>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>
